spring:
    cloud:
      config:
        server:
          git:
            uri: https://github.com/walidbe/lawyer-doodle/tree/master/config-repo
            ignoreLocalSshSettings: true
            
          
server:
  port: 8888